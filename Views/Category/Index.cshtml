@model IEnumerable<ExpensesWebApp.Models.Category>

@{
    ViewData["Title"] = "Categorias";
}

<h1>Categorias</h1>

<p>Veja aqui as categorias de despesa existentes</p>
<p>
    <a class="btn btn-primary" asp-action="Create"><i class="bi bi-plus"></i>&nbsp;Criar nova categoria</a>
</p>
@if (Model.Count() == 0)
{
    <div class="border d-flex align-items-center justify-content-center" style="height:150px">
        <h2 class="text-danger">Ainda não há categorias.</h2>
    </div>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-dark">
                            <i class="bi bi-pencil-square"></i>&nbsp;Editar
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">
                            <i class="bi bi-trash3"></i>&nbsp;Excluir
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
